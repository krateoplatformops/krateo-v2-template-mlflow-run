repo:
  owner: generic-provider-testing
  workflow_id: "run.yaml"
  ref: main

mlflow:
  experiment:
    name: "test-exp-gen"
  credentials:
    secretRef:
      namespace: mlflow-system
      name: azure-token
      key: access_token
  run:
    name: "mlflow-run-default"
    alpha: "0.5"
    metrics:
      - name: "mse"



